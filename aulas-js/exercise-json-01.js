const person = {
    name: 'maurício moreira neto',
    age: 33,
    occupation: 'professor'
};

console.log(`Hello, my name is ${person.name} and i has ${person.age} years old and my occupation is ${person.occupation}.`);